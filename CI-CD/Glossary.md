
## ⚙️ Kubernetes (k8s)

**Что это:**
Система оркестрации контейнеров для автоматизации развертывания, масштабирования и управления приложениями.

Это декларативная (**declarative**) система потому что ты описываешь **желаемое состояние** (desired state) в YAML, а Kubernetes контроллеры приводят фактическое состояние к нему. То есть ты не управляешь вручную, а лишь “описываешь результат”.

**Ключевые понятия:**

* **Pod** — минимальная единица развертывания (1 или несколько контейнеров с общими ресурсами и сетью.).
* **Deployment** — управляет репликами подов и обновлениями.
* **Service** — Дает стабильный IP и DNS для доступа к подам внутри кластера.
* **Ingress** — маршрутизация внешнего HTTP/HTTPS-трафика к сервисам (reverse proxy + TLS).
* **ConfigMap** - конфигурации (нечувствительные данные).
* **Secret** — пароли, токены, сертификаты (base64, защищенные).
* **Namespace** — изоляция ресурсов по проектам.
* **StatefulSet / DaemonSet / Job** — типы контроллеров для разных сценариев.
* **RBAC** — ролевая модель доступа (ClusterRole, RoleBinding).
* **Helm / ArgoCD / Flux** — инструменты для декларативного управления (см. ниже).

---

### Разница между Deployment и StatefulSet?

> Deployment — для статeless приложений (Nginx, API).
> StatefulSet — для stateful сервисов (БД, Kafka), сохраняет порядок и имена подов.

### Почему не стоит хранить `kubectl apply` манифесты без контроля версий?

> Потеряется reproducibility и аудит. GitOps (Argo/Flux) решает это — Git становится “источником правды”.

### Что такое “Reconciliation loop” в Kubernetes?

> Контроллер постоянно сверяет реальное состояние кластера с описанным (desired state).
> Это основа **self-healing** — если под упал, Kubernetes его перезапустит.

### Что делать, если Pod остаётся в статусе “CrashLoopBackOff”?

> Проверить:
>
> 1. `kubectl logs pod`
> 2. `kubectl describe pod`
> 3. LivenessProbe/ReadinessProbe
>    Часто причина — неправильный env или init container.

### Как безопасно обновлять приложение в Kubernetes?

> Через `rolling update` в Deployment.
> Можно задать `maxUnavailable` и `maxSurge`, чтобы не было простоя.

### Что делает `ReadinessProbe` и `LivenessProbe`?

> * **LivenessProbe** — перезапускает под, если он завис.
> * **ReadinessProbe** — убирает под из трафика, если он не готов.

### Что такое GitOps?

> Практика, когда Git — единственный источник истины для инфраструктуры.

### Что произойдёт, если удалить Pod вручную?

> ReplicaSet создаст новый — Kubernetes автоматически восстановит состояние.

### Что означает статус Pod “CrashLoopBackOff”?

> Контейнер падает при старте — нужно смотреть `kubectl logs` и `describe`.

---

## 🧩 Helm

**Что это:**
Пакетный менеджер для Kubernetes. Аналог apt/yum, но для k8s-манифестов.

**Ключевые концепции:**

* **Chart** — шаблон (набор YAML-манифестов + values.yaml).
* **Release** — установленный экземпляр Chart’а.
* **Values.yaml** — параметры для настройки деплоя.
* **Templating** — Go-шаблоны, позволяющие переиспользовать код.
* **Repositories** — хранение пакетов Helm (например, Harbor).

**Пример команды:**

```bash
helm install my-app ./chart -f values-prod.yaml
```

---
### ❓Что делает Helm?

> Пакетный менеджер для Kubernetes — упрощает установку и обновление приложений через чарт-файлы.

### ❓Что такое Chart?

> Набор YAML-шаблонов (манифестов) и `values.yaml` с параметрами.

### ❓Что такое Release?

> Установленный экземпляр чарта в кластере.

### ❓Что произойдёт, если удалить Helm release вручную через kubectl?

> Helm потеряет контроль над релизом (release metadata останется в Secret).
> При следующем апдейте Helm может “сломать” состояние.
> Лучше удалять через `helm uninstall`.

### ❓Как понять, что Helm-чарт или ArgoCD конфигурация работает корректно?

> Проверить через `kubectl describe`, `kubectl get events` и состояние `Pods/ReplicaSets`.
> ArgoCD покажет состояние “Synced/Healthy”.
### ❓Как обновить Helm release?

> `helm upgrade my-app ./chart -f values.yaml`


| Параметр      | Helm                                      | Kustomize                |
| ------------- | ----------------------------------------- | ------------------------ |
| Шаблоны       | Go-шаблоны (`{{ }}`)                      | Патчи YAML               |
| Подходит для  | Многократных инсталляций с разными values | Простых модификаций YAML |
| Использование | `helm install`                            | `kubectl apply -k`       |

**Ответ:** Helm — как “package manager”, Kustomize — “patching tool”.
В GitOps чаще применяют Helm для CI/CD.

---
## 🚀 Argo CD

**Что это:**
GitOps-инструмент для CD в Kubernetes.
Argo синхронизирует состояние кластера с Git-репозиторием.

**Особенности:**

* Декларативное управление инфраструктурой (Git — источник правды).
* Автоматическая синхронизация манифестов.
* Визуальный дашборд состояния приложений.
* Поддержка Helm-чартов и Kustomize.
* RBAC, Single Sign-On, health checks.

**Пример:**
Git содержит `manifests/` → ArgoCD следит → применяет изменения → откатывает при ошибке.

---
### ❓Что делает ArgoCD?

> Реализует GitOps — автоматически синхронизирует кластер с Git-репозиторием манифестов.

### ❓Преимущества?

> Rollback, health check, visual dashboard, auto-sync, RBAC.

### ❓Что значит “Git — single source of truth”?

> Источник правды — только Git, состояние кластера должно совпадать с репозиторием.

### ❓ArgoCD vs Flux — какой выбрать?

* **ArgoCD** — GUI, визуализация, ручные sync, удобно для команд.
* **Flux** — полностью декларативный, CLI-only, легче автоматизировать.

> В продакшне часто используют ArgoCD для приложений, Flux — для системных компонентов.

### ❓Как ArgoCD узнаёт, что в Git изменился манифест?

> Через **polling** или **webhook** из GitLab/GitHub.
> После этого ArgoCD пересинхронизирует состояние кластера.

### ❓Можно ли комбинировать Helm и ArgoCD?

> Да. ArgoCD умеет деплоить Helm-чарты напрямую (`source.helm`),
> и управлять values через Git.

### Что делать, если ArgoCD показывает “Out of Sync”, но кластер в порядке?

> Значит, кто-то изменил ресурс вручную через `kubectl`.
> Решение — включить **auto sync** или запретить прямые изменения.

---

## 🧭 Flux CD

**Что это:**
Альтернатива ArgoCD для GitOps-деплоя.
Ориентирован на модульность и простоту CLI.

**Ключевые фичи:**

* Контроллеры для Git, Helm, Image automation.
* Автоматические обновления версий образов.
* Совместим с Kustomize, Helm и OCI-репозиториями.
* Подходит для “multi-cluster” деплоя.

**Flux vs ArgoCD:**

| Особенность             | ArgoCD          | Flux                  |
| ----------------------- | --------------- | --------------------- |
| UI                      | ✅ Да            | ⚪ Нет (CLI + Grafana) |
| Rollback                | ✅ Да            | ✅ Да                  |
| Авто-обновление образов | ⚪ Через плагины | ✅ Встроено            |
| Интеграция с Helm       | ✅               | ✅                     |

---
### ❓Flux CD — это что?

> Легковесный GitOps контроллер без UI, полностью декларативный.

### ❓Разница с ArgoCD?

> Argo имеет UI, Flux — CLI и YAML. Flux сильнее в автообновлении Docker-образов.

### ❓Поддерживает Helm?

> Да, Flux может автоматически обновлять Helm-чарты из репозиториев.

---

## 🏗️ Harbor

**Что это:**
Enterprise-реестр контейнеров и Helm-чартов.
Альтернатива Docker Hub / GitLab Registry с расширенным контролем безопасности.

**Особенности:**

* Поддержка **Docker Registry API**, **OCI Artifacts**, **Helm Charts**.
* RBAC, SSO, репликация между инстансами.
* Встроенное **сканирование уязвимостей** (Trivy / Clair).
* Политики подписи и репликации образов.
* Интеграция с CI/CD и Kubernetes.

**Пример использования:**

```bash
docker push harbor.company.com/project/app:1.0
helm repo add harbor https://harbor.company.com/chartrepo/project
```

---
### ❓Что такое Harbor?

> Enterprise-реестр контейнеров и Helm-чартов, аналог Docker Hub, но с безопасностью и RBAC.

### ❓Основные фичи?

> RBAC, сканирование уязвимостей, репликация образов, SSO, подпись артефактов.

### ❓Как использовать?

> `docker push harbor.local/project/app:1.0`
> `helm repo add harbor https://harbor.local/chartrepo/app`

### ❓Что делает Harbor, чего нет в Docker Hub?

> RBAC, SSO, репликация между кластерами, подписи артефактов, уязвимости, retention policies.
> Docker Hub — это просто хостинг образов, Harbor — enterprise-registry.

---

## 📊 Apache Superset

**Что это:**
BI-платформа с открытым исходным кодом для визуализации данных и аналитики.

**Ключевые возможности:**

* Подключение к БД (Postgres, ClickHouse, MySQL и др.).
* SQL Lab для интерактивных запросов.
* Dashboard-билдер с drag-and-drop.
* Аутентификация (OAuth, LDAP).
* Role-based Access Control.
* REST API для автоматизации и CI/CD интеграций.

**Используется:**
для мониторинга метрик, аналитики данных в DevOps и ML-проектах.

---
### ❓Что делает Superset?

> BI-платформа для визуализации данных и SQL-аналитики.

### ❓Где используется в DevOps?

> Мониторинг логов, дашборды метрик, аналитика бизнес-показателей.

---

## 🧾 K8s / OpenShift манифесты

**Что это:**
YAML-декларации, описывающие состояние ресурсов в кластере.

**Типы манифестов:**

* **Pod**, **Service**, **Deployment**, **ConfigMap**, **Ingress**, **Secret**, **PersistentVolumeClaim**.
* OpenShift добавляет: **Route**, **BuildConfig**, **DeploymentConfig**, **ImageStream**.

**Пример Deployment:**

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
  template:
    metadata:
      labels:
        app: my-app
    spec:
      containers:
      - name: app
        image: my-app:latest
        ports:
        - containerPort: 8080
```

---
### ❓Что такое манифесты?

> YAML-декларации состояния ресурсов Kubernetes.

### ❓В чем отличие OpenShift от Kubernetes?

> OpenShift = Kubernetes + CI/CD + RBAC + встроенные образы и Route вместо Ingress.

### ❓Как применить манифест?

> `kubectl apply -f deployment.yaml`

---

