В PostgreSQL есть четыре уровня изоляции транзакций, которые определяют, как параллельные транзакции взаимодействуют друг с другом и какие изменения данных они видят. Эти уровни соответствуют стандарту SQL и управляют проблемами 
- **грязного чтения**
- **неповторяемого чтения** 
- **фантомных чтений**

Неповторяемое чтение — транзакция повторно читает те же данные, что и раньше, и обнаруживает, что они были изменены другой транзакцией (которая завершилась после первого чтения).

Фантомное чтение — транзакция повторно выполняет запрос, возвращающий набор строк для некоторого условия, и обнаруживает, что набор строк, удовлетворяющих условию, изменился из-за транзакции, завершившейся за это время.

---

## **1. READ UNCOMMITTED** *(чтение неподтверждённых данных)*

⚠️ **В PostgreSQL фактически работает как READ COMMITTED** — настоящих грязных чтений нет.

* **Описание:**
  Транзакция может видеть изменения, сделанные другими транзакциями, даже если они ещё не зафиксированы.
  Однако PostgreSQL автоматически повышает этот уровень до **READ COMMITTED**, поэтому поведение идентично следующему уровню.
* **Проблемы:**

  * Грязные чтения — **невозможны** в PostgreSQL.
  * Неповторяемые чтения — **возможны**.
  * Фантомные чтения — **возможны**.
* **Используется редко**, так как по факту совпадает с READ COMMITTED.

```sql
SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;
```

---

## **2. READ COMMITTED** *(чтение подтверждённых данных)* ✅ **уровень по умолчанию**

* **Описание:**
  Каждый запрос внутри транзакции видит **только те изменения**, которые были зафиксированы **до начала запроса**, а не транзакции в целом.
* **Пример:**
  Если внутри транзакции два SELECT'а, то второй может увидеть изменения, которые были закоммичены другой транзакцией **между этими SELECT'ами**.
* **Проблемы:**

  * Грязные чтения — **невозможны**.
  * Неповторяемые чтения — **возможны** (результаты одного и того же запроса могут отличаться внутри одной транзакции).
  * Фантомные чтения — **возможны**.
* **Когда использовать:**
  Подходит для большинства веб-приложений, где важна производительность, а не полная консистентность.

```sql
SET TRANSACTION ISOLATION LEVEL READ COMMITTED;
```

---

## **3. REPEATABLE READ** *(повторяемое чтение)*

* **Описание:**
  Все SELECT внутри транзакции работают **на одном и том же снимке данных**.
  Изменения, сделанные другими транзакциями, после начала текущей — **не видны**.
* **Поведение:**

  * Если в начале транзакции прочитали запись, то при повторном SELECT эта запись будет **в том же состоянии**.
  * Новые строки, добавленные другой транзакцией, **не видны**.
* **Проблемы:**

  * Грязные чтения — **невозможны**.
  * Неповторяемые чтения — **невозможны**.
  * Фантомные чтения — **невозможны** в PostgreSQL (в отличие от некоторых других СУБД, например MySQL).
* **Когда использовать:**
  Подходит, если нужны стабильные данные на протяжении транзакции.

```sql
SET TRANSACTION ISOLATION LEVEL REPEATABLE READ;
```

---

## **4. SERIALIZABLE** *(сериализуемый уровень)* 🔒 **самый строгий**

* **Описание:**
  Гарантирует, что результат выполнения параллельных транзакций **эквивалентен** их последовательному запуску.
  Используется механизм **Serializable Snapshot Isolation (SSI)**.
* **Поведение:**

  * Полностью предотвращает фантомные и неповторяемые чтения.
  * Если конфликт невозможно разрешить, PostgreSQL откатывает одну из транзакций с ошибкой `serialization_failure`.
* **Минусы:**

  * Сильное падение производительности при высокой конкурентности.
  * Может потребоваться перезапуск транзакции при конфликте.
* **Когда использовать:**
  Для финансовых операций, биллинга, критически важных расчётов.

```sql
SET TRANSACTION ISOLATION LEVEL SERIALIZABLE;
```

---

## **Сравнительная таблица**

| Уровень              | Грязные чтения | Неповторяемые чтения | Фантомные чтения | Снимок данных               |
| -------------------- | -------------- | -------------------- | ---------------- | --------------------------- |
| **READ UNCOMMITTED** | ❌              | ✅                    | ✅                | Нет                         |
| **READ COMMITTED**   | ❌              | ✅                    | ✅                | На момент запроса           |
| **REPEATABLE READ**  | ❌              | ❌                    | ❌ *(в PG)*       | На момент начала транзакции |
| **SERIALIZABLE**     | ❌              | ❌                    | ❌                | Логическая сериализация     |

---

## **Рекомендации**

* Если нужна **производительность** → используйте **READ COMMITTED** *(по умолчанию)*.
* Если нужна **стабильность данных в транзакции** → **REPEATABLE READ**.
* Если нужна **полная консистентность** → **SERIALIZABLE**, но нужно обрабатывать `serialization_failure`.

---
