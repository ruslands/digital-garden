Barish

Thorne

Rainer Weiss

Maxwell Equations

dx/dt - это одно из обозначений производной от x по t

dx/dt is a differential element of a function “x” that changes with respect to time.

Inertional frame

[Inertial frame of reference](https://en.wikipedia.org/wiki/Inertial_frame_of_reference)

To log scale

Это же несложно. Логарифмическая шкала, значит первое преобразование - взятие логарифма:

x' = log10x 

Получили преобразование диапазона [1.14E-23, 2.428272E+35] в [-22.94309515, 35.38529733]. Теперь надо уложить полученный диапазон в [10, 60000] линейным преобразованием:

x'' = (x' - (-22.94309515)) / (35.38529733 - (-22.94309515)) * (60000 - 10) + 10

Сводя преобразования вместе получаем

x'' = (log10x + 22.94309515) * 1028.487113 + 10

Да, мне просто надо предвычислить для всех значений (log10x - log10min)/(log10max - log10min) и хранить все эти предвычисленные значения, а потом просто умножать их на параметры требуемой шкалы и сдвигать по ней. Действительно просто и логично.

Вобщем спасибо.

EM: A general algorithm for ML estimation with “missing data”.

Формула Бернулли — формула в теории вероятностей, позволяющая находить вероятность появления события. определённое количество раз при нескольких независимых испытаниях.

Логарифмы полезны для решения уравнений, в которых неизвестные присутствуют в качестве показателя степени.

/---Optimization---/

методы нулевого порядка, не требующие вычисления производной

одномерная оптимизация - поиск минимума функции с одной переменной

многомерная оптимизация - поиск минимума функции с более, чем одной переменной

условная оптимизация

безусловная оптимизация - такие задачи, при формулировке которых на значения аргументов налагаются ограничения в виде равенств или неравенств.

Аргуме́нт минимиза́ции (argmin или arg min) — аргумент, при котором данное выражение достигает минимума

Экстремум функции - минимум или максимум

1. Необходимое условие экстремума: (производные могут быть равны 0, но экстремума там нет)

Первые частные произовдные функции в точке равны 0.

2. Достаточное условие экстремума:

Взять вторые производные по x,y,xy

[https://www.youtube.com/watch?v=WRbp5IcUrtM](https://www.youtube.com/watch?v=WRbp5IcUrtM)

chain rule is a formula for computing the derivative of the composition of two or more functions.

The angled brackets represent an inner product. The best known one is the scalar product or the dot product. If u=(u1,u2,u3) and v=(v1,v2,v3), then the dot product is given by ⟨u,v⟩=u1v1+u2v2+u3v3.

/---------Sequence---------/

convergence - сходимость

ключевая задача - исследование ряда на сходимость. Возможны 2 случая - ряд расходиться и ряд сходиться.

Ряд расходится - значит, что бесконечная сумма равна бесконечности

Ряд сходится - значит, что бесконечная сумма равна некоторому конечному числу

Однако в подавляющем большинстве случаев найти сумму ряда не так-то просто, и поэтому на практике для исследования сходимости ряда используют специальные признаки, которые доказаны теоретически.

Существует несколько признаков сходимости ряда: необходимый признак сходимости ряда, признак сравнения(с неотрицательными членами), предельный признак сравнения(с отрицательными членами), признак Даламбера, признаки Коши, признак Лейбница и некоторые другие признаки.

необходимый признак сходимости ряда:

для того, чтобы ряд сходился, необходимо, чтобы его общий член стремился к нулю. И всё бы было отлично, но этого ещё не достаточно. Иными словами, если общий член ряда стремится к нулю, ТО ЭТО ЕЩЕ НЕ ЗНАЧИТ, что ряд сходится – он может, как сходиться, так и расходиться!

Признак сравнения:

Предельный признак сравнения:

Настоящей «рабочей лошадкой» числовых рядов является предельный признак сравнения, и по частоте использования с ним может конкурировать разве что признак Даламбера.

Обычный числовой ряд состоит из чисел

Функциональный же ряд состоит из ФУНКЦИЙ

степенной ряд-это наиболее популярная разновидность функционального ряда

Convergence rate - the speed at which a convergent sequence approaches its limit.

Convergence order - число

/---------CONJUGATE DIRECTION METHODS(Powell method)Метод сопряженных направлений--------/

Это итерационный численный метод (первого порядка) решения оптимизационных задач, который позволяет определить экстремум (минимум или максимум) целевой функции.

The class of conjugate direction methods can be viewed as being intermediate between the method of steepest descent and Newton’s method.

/---QUASI-NEWTON METHODS---/

Newton’s method is one of the more successful algorithms for optimization. If it converges, it has a quadratic order of

convergence.

/---------Linear Algebra--------/

Линейное(Векторное) пространство множество всех векторов с введеными на этой плоскости операциями сложения, умножения, вычитания.

v1,v2,vn - вектора линейно зависимые если найдутся такие коэффиценты a, что

a1v1+a2v2+anvn=0

/-Матрицы-/

E - единичная матрица(единицы по диагонали)

Ассоциативность (A*B)*C = A*(B*C)

Дистрибутивность A*(B+C) = A*B + A*C

От перемены мест множителей произведение может поменяться

Умножение матриц Anm x Bmk = Cnk

/-------Дифферинцирование-------/

Дифферинцирование - поиск производной

Производная сложной функции (u(v))' - когда одна функция вложена в другую, u-внешняя функция, v-вложенная функция.

Сначала нужно найти производную от внешней функции, затем от внутренней.

Полный дифференциал - сумма частных производных

dF/dx - F - main function, x - variable.

/-------Интегрирование-------/

Решить дифференциальное уравнение, значит восстановить исходную функцию (общий интеграл)

/----Квадратичные формы----/

Линейной формой n переменных называют однородный многочлен 1-й степени:

L(x1,x2,x3,...,xn) = a1x1 + a2x2 + a3x3 + ... + anxn

Например L(x1,x2) = x1 - 2x2 - линейная форма двух переменных

Квадратичной формой n переменных называют однородный многочлен 2-й степени,

каждое слагаемое которого содержит либо квадрат переменной, либо парное произведение переменных.

Q(x1,x2) = a11x1^2 + a22x2^2 + 2a12x1x2

a11 – в этом слагаемом находится  произведение x1 и x1 (квадрат);

a22 – здесь произведение x2*x2;

a12 – и здесь произведение x1*x2;

Матрицу А, составленную из этих коэффициентов, называют матрицей квадратичной формы.

Больше ничего – никаких «одиноких иксов» и никакой приплюсованной константы (тогда уже получится не квадратичная форма, а неоднородный многочлен 2-й степени).

Знакоопределённость квадратичной формы. Критерий Сильвестра

Теорема (критерий Сильвестра). Квадратичная форма является положительно определенной тогда и только тогда, когда все главные миноры матрицы этой формы положительны.

Еще одна теорема. Если ВСЕ собственные значения матрицы квадратичной формы положительны, то она определена положительно. Если все отрицательны – то отрицательно.

Исследовать на знакоопределенность квадратичную форму можно двумя способами - через характеристическое уравнение или через вычисление главных миноров.

Тип квадратичной формы можно определить, не приводя ее к каноническому виду. Следующий ниже критерий Сильвестра позволяет определить тип квадратичной формы по знакам угловых миноров ее матрицы.

Ранг матрицы определяется по кол-ву ненулевых миноров.

Если ВСЕ угловые миноры матрицы формы больше нуля,

Теперь, собственно, критерий:

1) если ВСЕ угловые миноры матрицы формы больше нуля, то она определена положительно, если они не отрицательны – то неотрицательно.

2) если миноры знакочередуются, причём, первый минор отрицателен, то квадратичная форма является отрицательно определённой. Если нечётные миноры неположительные, а чётные неотрицательные.

/-----Convex-----/

The neighborhood is the ball with radius e and center x.

/--Безусловная оптимизация / Unconstrained Optimization--/

Унимодальная функция - функции, у которых каждый локальный минимум является одновременно и глобальным.

/--Математическое программирование--/

Математическое программирование - математическая дисциплина, посвященная теории и методам решения задач о нахождении экстремумов функций на множествах конечномерного векторного пространства, определяемых линейными и нелинейными ограничениями (равенствами и неравенствами).

Математически ограничения выражаются в виде уравнений и неравенств.  Их совокупность образует область допустимых  решений.

Объединение  всех  условий  (ограничений),  налагаемых  на  неизвестные  (искомые)  величины xi задачи, обозначим буквой Ω (X∈Ω).

В зависимости от особенностей целевой функции Z(x) и функций задающих ограничения, в математическом программировании принято выделять ряд разделов:

Линейное программирование

Нелинейное программирование

Динамическое программирование

Дискретное программирование

Параметрическое программирование

Стохастическое программирование

Геометрическое программирование

Необходимым условием экстремума функции нескольких переменных является равенство нулю ее частных производных.

Первая производная в точке экстремума равна 0

метода первого порядка, то есть такого, который использует производную рассматриваемой функции.

Чем больше информации об исследуемой функции в процессе минимизации используется, тем быстрее достигается результат. Таким образом, скорость сходимости процесса минимизации увеличивается, но попутно возрастает количество необходимых вычислений. Так, из рассмотренных методов, медленнее всего сходятся методы нулевого прядка, далее

идут методы первого, а затем второго порядка. Однако, например, в методе касательных, в отличие от метода золотого сечения, приходится вычислять не только значение рассматриваемой функции, но и ее производную, а в методе Ньютона-Рафсона еще и вторую производную.

Вопросы:

Минимизировать скалярную функцию векторного аргумента

/--Ранг матрицы--/

1. Находим не равные нулю миноры второго порядка. Если все миноры второго порядка равны нулю, то ранг матрицы будет равен единице (r =1).

2. Если существует хотя бы один минор второго порядка, не равный нулю, то составляем окаймляющие миноры третьего порядка. Если все окаймляющие миноры третьего порядка равны нулю, то ранг матрицы равен двум (r =2).

3. Если хотя бы один из окаймляющих миноров третьего порядка не равен нулю, то составляем окаймляющие его миноры. Если все окаймляющие миноры четвёртого порядка равны нулю, то ранг матрицы равен трём (r =2).

4. Продолжаем так, пока позволяет размер матрицы.

Класс задач P определяется довольно просто — это набор таких задач, для которых мы знаем алгоритм, работающий за полиномиальное время. Для простоты можно считать, что класс P — это класс простых задач, поскольку мы знаем быстрые алгоритмы для их решения. Примеры задач из класса P: сложение двух чисел, сортировка множества из n элементов, поиск элемента в некотором множестве, выяснение связности графа и так далее. Напротив, класс NP определяется как задачи, для которых мы можем лишь проверить некоторое решение за полиномиальное время, но нам неизвестен алгоритм, который мог бы решить задачу настолько же быстро. Примеры задач из класса NP: задача коммивояжёра, разложение числа на простые множители.Ещё более сложные задачи — это задачи из класса NP-complete. Если вам удаётся свести решение задачи А к решению задачи Б, то последняя задача как минимум настолько же сложная, как и первая. Суть NP-complete класса в том, что к любой задаче из данного класса можно свести абсолютно все задачи из класса NP, то есть каждая задача из данного класса настолько же сложна, как и любая задача из NP.  

  
/--Математический Анализ--/

ℕ — множество натуральных чисел: 1, 2, 3, ....  
ℤ — множество целых чисел.  
ℚ — множество рациональных чисел, то есть дробей вида mn, где m∈ℤ и n∈ℕ  
ℝ — множество вещественных (действительных) чисел. Возникший из потребности измерения геометрических и физических величин окружающего мира  
ℂ — множество комплексных чисел.  
Запись {x∈X:некоторое условие на x} означает подмножество множества X, состоящее из элементов, удовлетворяющих указанному условию.  
Запись ℝ×ℝ означает множество всех пар вещественных чисел.  
Записи A∪B и A∩B обозначают соответственно объединение и пересечение множеств A и B  
Записи A∖B обозначают разность множеств, т.е. A∖B состоит из таких элементов множества A, которые не принадлежат множеству B  
∀ — квантор всеобщности, используется для сокращенной записи вместо слов “каждый“, “любой”, или “для всякого“, “для любого“ и т. п..  
∃ — квантор существования, используется для сокращенной записи вместо слов “найдется“, “существует“ и т. п..  
f:X→Y — функция, заданная на множестве X, множество значений которой лежит в Y (но необязательно с ним совпадает).  

угловыми скобками обозначают — скалярное произведение векторов  
|| x ||1 = L1 норма  
|| x ||2 = L2 норма - корень суммы квадратов  

/-----------Последовательности-----------/  
ℕ→ℝ - Последовательность это отображение множества натуральных чисел во множество вещественных чисел  

Предел последовательности  
limxn→L - L есть предел последовательности xn или последовательность xn сходится к L или  
последовательность xn стремится к L  

Lim min{Xn,Yn} - При каждом n берем наименьшее из чисел xn и yn. Получаем последовательность.

Для любого интервала содержащего точку L, вне этого интервала находится лишь конечное число членов последовательности.

Последовательность не может иметь двух различных пределов

Арифмитические действия с пределами  
LimXn = a, LimYn = b          
Lim(Xn+Yn) = a+b  
Lim(Xn-Yn) = a-b  
Lim(XnYn) = ab  
Lim(Xn/Yn) = a/b

Xn бесконечно большая последовательность, если LimXn = inf  
Xn бесконечно малая последовательность, если LimXn =

Неограниченной считается последовательность, не имеющая границы хотя бы с одной стороны.  
Бесконечно большая - всегда неограниченная, но неограниченная может не быть бесконечно большой  
бесконечно большая последовательность отличается от неограниченной тем что у бесконечно большой каждый следующий член по модулю больший предыдущего.  
Свойства бесконечно малых последовательностей:  
1. Бесконечно малая последовательность всегда ограничена  
2. Сумма, разность, произведение БМП - бесконечно малы  
3. Произведение БМП и ограниченной последовательности - бесконечно малая

Арифмитические действия с бесконечностями  
LimXn = +inf, Yn - ограничена снизу => Lim(Xn+Yn) = +inf  
LimXn = -inf, Yn - ограничена сверху => Lim(Xn+Yn) = -inf

Запрещенные операции  
-inf + (+inf)  
+inf - (+inf)  
0*inf  
0/  
inf/inf        

Аксиома - Положение, принимаемое без доказательств.

Supremum - наименьшая из верхних границ(точная верхняя граница)  
Infimum - наибольшая из нижних границ(точная нижняя граница)

для множества (−1;0) верхней границей является любое неотрицательное число, но наименьшей из верхних границ, то  
есть супремумом, является ноль  

т.е., попросту, супремум - значение максимального элемента множества?  
 максимального элемента у множества может и не быть. Как, например, в примере выше -- ноль не является элементом множества.  
но если наибольший элемент есть, то супремум с ним совпадает. Как, например, для множества [−1,0].  

можно разбить множество на 2 подпоследовательности: нечетных n и четных n (это поможет избавиться от  (−1)n). Затем показать, что одна убывает, а вторая возрастает. Тогда по определению убывающей/возрастающей последовательности первый элемент убывающей будет sup, а возрастающей - inf.  

/------Производная------/  
Приращение аргумента дельта икс  
Приращение функции дельта игрек  
Производная функции в точке это число, которое показывает убывает/возрастает функция.  
Определение производной базируется на понятии предела.  
Сначала применяем правило дифференцирования потом таблицу производных.  
y'(x)=dy/dx  
Точки экстремума – это «иксовые» значения.  
Экстремумы – «игрековые» значения.  
Непрерывность функции.  
Функция должна быть определена в точке x, то есть должно существовать значение f(x)  

    На вопрос: чему равен х в уравнении  
3^x = 8 ?  
Мы честно отвечаем: х равен числу, в которое надо возвести 3, чтобы получить 8! Или, чтобы так долго не говорить, пишем в сокращённом варианте, через логарифм:  
х = log3

Читаем ещё раз: "икс равен логарифму восьми по основанию три".

Производная - скорость изменения функции

/---------------SVD (singular value decomposition------------/  
    
Произвольную прямоугольную матрицу представить в виде произведения трех матриц. F = VDU(T)  
   V - ортогональная. Столбцы являются собственными векторами  
   Ut - ортогональная. row factors, columns items  
   D - Диагональная матрица. По диагонали записаны собственные значения матриц F(T)F и FF(T)  
   
Ортогона́льная ма́трица — квадратная матрица A с вещественными элементами, результат умножения которой на транспонированную матрицу A(T) равен единичной матрице.  
Едини́чная ма́трица — квадратная матрица, элементы главной диагонали которой равны единице поля, а остальные равны нулю.  
Разрежённая матрица(sparse) — это матрица с преимущественно нулевыми элементами. В противном случае, если бо́льшая часть элементов матрицы ненулевые, матрица считается плотной.  
Ранг матрицы равен количеству ненулевых строк после приведения матрицы к ступенчатому виду.  
Сингулярные числа - корень от собственных чисел матрицы  
/---Теория множеств---/  
   
 A C B - означает «каждый элемент из A также является элементом из B  
     
 A => B - означает «если  A верно, то B также верно».  
     
     
Лине́йная а́лгебра — часть алгебры, изучающая векторы, векторные, или линейные пространства, линейныеотображения и системы линейных уравнений.  

1. Определитель матрицы, детерминант матрицы |x,y| = detA  
   Определитель - это число, показывающее площадь или объем под векторами.  
 - определитель можно найти только у квадратной матрицы - кол-во строк = кол-во столбцов

для матрицы 2х

|a b|  
 |c d| = detA = a*d - b*c  
   
 для матрицы 3х

Метод треугольника

|a b c|  
 |d e f|  
 |g h j| = a*e*f + b*f*g + d*h*c - c*e*g - d*b*j - a*f*h

Метод разложения по строке

2. Операции с матрицами

- Сложение матрицы Сij = Aij + Bij  
 - Умножение на число (  
 - Произведение двух матриц

3. Собственный вектор и собственные значения (Eigenvector eigenvalues)