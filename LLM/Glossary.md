## ⚡ vLLM

Быстрый inference движок LLM.
**PagedAttention** — экономия GPU памяти.
OpenAI-совместимый API `/v1/completions`.

---

## 🧠 Triton (NVIDIA)

Inference server для PyTorch/TF/ONNX моделей.
**Фичи:** GPU batching, gRPC/HTTP, Prometheus metrics.
Используется в MLops пайплайнах.

---

## 🚀 SGLang

Оптимизированный runtime для LLM.
**Фокус:** multi-GPU, низкая задержка, OpenAI API.
Похож на vLLM, но быстрее в некоторых сценариях.

---

## 🔗 LangChain

Фреймворк для создания LLM-приложений.
**Компоненты:** Chains, Agents, Memory, Tools.
**Пример:** чат-боты, reasoning агенты, интеграция API.

---

## 📚 LlamaIndex

Интерфейс для подключения внешних данных к LLM (RAG).
**Компоненты:** Document, Index, Retriever.
Работает с FAISS, Qdrant, Weaviate.

## ⚡ vLLM

**Что это:**
High-performance inference engine для больших языковых моделей.

**Особенности:**

* Разработан для **Serving LLMs** (в отличие от обучения).
* **PagedAttention** — эффективно использует GPU память.
* Поддержка **OpenAI API совместимого сервера** (`/v1/completions`).
* Интеграция с **HuggingFace Transformers** и **Triton Inference Server**.
* Масштабирование через **tensor/async parallelism**.

**Пример:**

```bash
python -m vllm.entrypoints.openai.api_server --model meta-llama/Llama-3-8B
```

---

## 🚦 Triton Inference Server (NVIDIA)

**Что это:**
Сервер для продакшн-инференса моделей (LLM, CV, ASR и др.).

**Поддерживает:** TensorRT, PyTorch, ONNX Runtime, TensorFlow, vLLM.

**Функции:**

* Мульти-фреймворк архитектура.
* Автоматическое масштабирование и batching.
* gRPC / HTTP API.
* Prometheus-метрики.
* GPU-оптимизация через CUDA / TensorRT.

**Используется:**
в MLops пайплайнах для централизованного inference сервиса.

---

## 🧮 SGLang

**Что это:**
Runtime-фреймворк для ускоренного исполнения LLM (аналог vLLM, но с фокусом на **graph optimization** и **multi-GPU inference**).

**Особенности:**

* Optimized **graph-based** execution.
* Поддержка **streaming output**.
* Совместимость с **OpenAI API**.
* Поддержка **Llama**, **Mistral**, **Qwen**, **Phi** и др.

**Сценарий:**
Используется для production-LLM серверов с высокой производительностью и низкой задержкой.

---

## 🔗 LangChain

**Что это:**
Фреймворк для построения LLM-приложений (chain’ы, агенты, memory, tools).

**Ключевые модули:**

* **LLMs** — обёртки над OpenAI, Anthropic, HuggingFace.
* **Prompt Templates** — стандартизированные промпты.
* **Chains** — последовательности вызовов моделей.
* **Agents** — динамическое принятие решений.
* **Memory** — хранение контекста диалога.
* **Retrievers / Tools** — подключение внешних данных (API, БД).

**Пример:**

```python
from langchain.chains import LLMChain
from langchain.prompts import PromptTemplate
from langchain.llms import OpenAI

template = "Translate this to French: {text}"
prompt = PromptTemplate.from_template(template)
chain = LLMChain(llm=OpenAI(), prompt=prompt)
print(chain.run(text="Hello"))
```

---

## 📚 LlamaIndex (бывш. GPT Index)

**Что это:**
Фреймворк для подключения **внешних данных** к LLM через индексы.

**Основные элементы:**

* **Document / Node / Index** — структура данных.
* **VectorStore** — хранение эмбеддингов.
* **Retriever** — выбор релевантных документов.
* **Query Engine** — обработка вопросов.
* Поддержка **LangChain интеграции**, **Weaviate**, **Qdrant**, **FAISS**.

**Пример:**

```python
from llama_index import SimpleDirectoryReader, GPTVectorStoreIndex
docs = SimpleDirectoryReader("./data").load_data()
index = GPTVectorStoreIndex.from_documents(docs)
query_engine = index.as_query_engine()
response = query_engine.query("Summarize this document.")
```

---

## 🔍 Haystack

**Что это:**
Open-source NLP/LLM-фреймворк от deepset.ai для **retrieval-augmented generation (RAG)**.

**Особенности:**

* Компонентная архитектура (Retriever, Reader, Generator).
* Поддержка Elasticsearch, FAISS, Milvus.
* Подходит для чатов, Q&A, поисковых систем.
* Интеграция с LangChain и HuggingFace.
* REST API и FastAPI-интерфейс.

**Пример:**

```python
from haystack.nodes import EmbeddingRetriever
from haystack.document_stores import FAISSDocumentStore
from haystack.pipelines import DocumentSearchPipeline

doc_store = FAISSDocumentStore()
retriever = EmbeddingRetriever(document_store=doc_store, embedding_model="sentence-transformers/all-MiniLM-L6-v2")
pipeline = DocumentSearchPipeline(retriever)
result = pipeline.run(query="What is Kubernetes?")
```

---

## ✅ Итоговое сравнение LLM Frameworks

| Framework      | Назначение   | Особенности                                       |
| -------------- | ------------ | ------------------------------------------------- |
| **vLLM**       | Serving      | Оптимизирован для inference, OpenAI API совместим |
| **Triton**     | Serving      | Поддержка разных фреймворков, GPU оптимизация     |
| **SGLang**     | Serving      | Высокая скорость, multi-GPU                       |
| **LangChain**  | App Logic    | Chains, agents, memory                            |
| **LlamaIndex** | Data Layer   | RAG, индексы документов                           |
| **Haystack**   | RAG / Search | Компонентный пайплайн с retriever/reader          |

## ⚡ vLLM

### ❓Что делает vLLM?

> Высокопроизводительный inference движок для LLM (не обучение, а выдача ответов).

### ❓Главная фича?

> PagedAttention — экономия GPU памяти и ускорение генерации.

### ❓API-совместимость?

> Полностью совместим с OpenAI API (`/v1/completions`, `/v1/chat/completions`).

---

## 🎯 Triton Inference Server

### ❓Что это?

> Сервер NVIDIA для развертывания моделей (LLM, CV, ASR и др.) с поддержкой PyTorch, TensorRT, ONNX, TF.

### ❓Преимущества?

> Multi-framework, batching, GPU-оптимизация, Prometheus-метрики.

---

## 🚀 SGLang

### Для чего нужен?

> Оптимизированный runtime для LLM-инференса с поддержкой multi-GPU и graph execution.

### Особенности?

> Быстрее vLLM при сложных графах, стриминг, OpenAI API совместимость.

---

## LangChain

### Что делает LangChain?

> Фреймворк для построения LLM-приложений с цепочками (`Chains`), агентами (`Agents`), и памятью (`Memory`).

### Пример использования?

> Создание чат-ботов, reasoning систем, API-агентов.

---

## LlamaIndex

### Что делает?

> Соединяет внешние документы с LLM (RAG).
> Создает индексы и хранит эмбеддинги для поиска по контенту.

### С чем работает?

> FAISS, Qdrant, Weaviate, Pinecone, LangChain.

---
## Haystack

### Что делает Haystack?

> Open-source фреймворк для Retrieval-Augmented Generation и Q&A систем.

### Ключевые компоненты?

> Retriever (поиск документов) + Reader (чтение и ответ) + Generator (LLM).

---
## Сводная таблица

| Инструмент                 | Класс         | Основное назначение         |
| -------------------------- | ------------- | --------------------------- |
| **Kubernetes**             | Orchestration | Управление контейнерами     |
| **Helm**                   | Packaging     | Шаблоны и деплой приложений |
| **ArgoCD / Flux**          | GitOps        | Автодеплой из Git           |
| **Harbor**                 | Registry      | Хранение образов и чартов   |
| **Superset**               | BI            | Аналитика и визуализация    |
| **vLLM / SGLang / Triton** | Inference     | Быстрый LLM-сервер          |
| **LangChain**              | App Logic     | Построение LLM-приложений   |
| **LlamaIndex / Haystack**  | Data Layer    | RAG и поиск по документам   |

---

### vLLM vs HuggingFace Transformers — зачем vLLM?

> Transformers подходят для обучения и прототипирования,
> vLLM — для **serving** (высокопроизводительная выдача).
> Он оптимизирует GPU память и умеет стримить токены с минимальной задержкой.

---

### Что такое PagedAttention?

> Оптимизация внимания (Attention), где ключи и значения хранятся в “страницах” памяти.
> Позволяет сервить несколько запросов параллельно без переполнения GPU.

---

### vLLM vs SGLang?

| Параметр           | vLLM              | SGLang                  |
| ------------------ | ----------------- | ----------------------- |
| Оптимизация        | PagedAttention    | Graph Execution         |
| Производительность | Стабильно высокая | Лучше на Multi-GPU      |
| Совместимость      | OpenAI API        | OpenAI API              |
| Сложность          | Простая           | Более сложная настройка |

---

### Triton vs vLLM — зачем оба?

> Triton — универсальный inference сервер (PyTorch, TF, ONNX).
> vLLM — узкоспециализирован под LLM.
> Можно запускать vLLM внутри Triton как backend.

---

### LangChain vs LlamaIndex — в чём разница?

> LangChain — про **логическую цепочку** (агенты, memory, tools).
> LlamaIndex — про **данные** (интеграция внешних документов, RAG).
> Часто они работают вместе: LlamaIndex подаёт контекст в LangChain.

---

### Haystack vs LangChain?

> Haystack — больше “инфраструктурный” фреймворк с пайплайнами и REST API.
> LangChain — более гибкий для кастомных агентов и логики.
> Для продакшна: Haystack, для прототипов: LangChain.

---

### Что такое RAG?

> **Retrieval-Augmented Generation** — LLM отвечает на вопросы,
> опираясь на внешние документы (retrieval + generation).

---

### Почему Triton популярен в продакшне?

> Он поддерживает **batching, metrics, scaling**, и легко встраивается в MLops пайплайны.
> Можно на одном сервере запускать десятки моделей разных типов.


### Как уменьшить время старта LLM inference?

> Использовать **vLLM с preloading**, **TensorRT-оптимизацию**,
> **lazy load** и **warmup requests** при запуске контейнера.

---

###  Что делает vLLM?

> Оптимизированный сервер inference для LLM, экономит GPU память (PagedAttention).

---

###  Что такое PagedAttention?

> Механизм памяти в vLLM, позволяющий обрабатывать несколько запросов без дублирования KV-кэша.

---

### Triton Inference Server — зачем нужен?

> Production-сервер от NVIDIA для запуска моделей (PyTorch, TF, ONNX) с GPU-оптимизацией.

---

### LangChain — ключевая идея?

> Конструктор LLM-приложений: объединяет модели, memory, агенты и внешние API в цепочки.

---

### LlamaIndex — зачем нужен?

> Связывает LLM с внешними документами (Retrieval-Augmented Generation, RAG).

---

### Haystack — чем отличается?

> Более инфраструктурный фреймворк RAG, с REST API и пайплайнами (Retriever, Reader, Generator).